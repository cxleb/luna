struct Test {
    a: int,
    b: number,
    c: string
}

func main() {
    let a = Test {
        a: 10,
        b: 10.10,
        c: "This is a string"
    };

    assert(a.a == 10);
    assert(a.b > 10.0);
    assert(a.b < 11.0);
    //assert(a.c == "This is a string");
}