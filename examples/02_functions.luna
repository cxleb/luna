func repeat(a: int, b: string) {
    let c = 0;
    while a > c {
        print_string(b);
        c = c + 1;
    }
}

func add(a: int, b: int) int {
    return a + b;
}

func main() {
    print_int(add(10, 20));
    repeat(10, "Hello, World!");
}
