func repeat(a, b) {
    let c = 0;
    while a > c {
        print(b);
        c = c + 1;
    }
}

func add(a, b) {
    return a + b;
}

func main() {
    print(add(10, 20));
    repeat(10, "Hello, World!");
}