- Add source length to AST
- Fix tokeniser(stop copying strings)
- String templates (blocker: interface, stdlib)
- Make functions a const value
- captures functions(closures?)
- Implement a concept of "api" so we can have std library
- Implement error handling(blocker: discriminated enums, generics)
- Generics
- Casting types
- Extensions (blocker: type method sets)
- For loops (do we want an iterator API for this?) (blocker: interfaces)
- Tasks (and Tasks API) (blocker: api)
- Refactor AST to be arena allocated
- Dont run GC on every check yield
- GC api?
- Fix panic to only kill the process, not the entire program
- Int ranges in switches 
- Const variables are const
- Refactor out string map in place of global map
- Refactor checker to return what kind of thing an expression evaluated to
- export syntax 

done:
- Implement multiple modules (import and export syntax?, how does the extension syntax work?)
- Assert should become a compiler construct, test condition and jump to panic block (blocker: debug info)
- Implement interfaces (use structual typing like go) (blocker: ir refactor, struct methods, method sets)
- Convert types to have a method set removing the need for structs to maintain a list of methods.
- Debug information is generated alongside bytecode
- Fix interface so it can either be a cli thing or embedded.
- refactor IR to remove need on type system
- Implement proper pattern matching
- Switch statement for non enums
- Switch for enum.
- Implement discriminated enums 
- struct methods
- Implement garbage collector
- Implement structs
- Builtin functions have function signatures
- Bug in AST source location
- Fix array types
- Adding source locations to AST
- Fix type system